%nav.navbar.navbar-default{:role => "navigation"}
  .container-fluid
    / Brand and toggle get grouped for better mobile display
    .navbar-header
      %button.navbar-toggle{"data-target" => ".main-navbar-collapse", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{:href => root_path} BJJMapper
    / Collect the nav links, forms, and other content for toggling
    .main-navbar-collapse.collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li{:class => action?(:map) ? 'active' : nil} 
          %a{:href => root_path, :data => { :'no-turbolink' => true }} Map
        %li{:class => action?(:index) ? 'active' : nil}
          %a{:href => locations_path} Academies
        %li{:class => action?(:meta) ? 'active' : nil}
          %a{:href => meta_path} About
      %form.navbar-form.navbar-left{:role => "search"}
        .form-group
          %input.form-control{:placeholder => "Search", :type => "text"}/
        %button.btn.btn-default{:type => "submit"} Submit
      = render partial: 'profile_or_login'
    / /.navbar-collapse
  / /.container-fluid
