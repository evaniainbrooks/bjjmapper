- new_instructor = User.new(teams: [team]).decorate
.tab-content
  %div.tab-pane#create-instructor
    = form_for([team, new_instructor], url: team_instructors_path(team), html: { class: 'form editable edit-mode' }) do |f|
      .container-fluid
        .row
          .col-md-4
            %div.text-center
              %img.center-block{:style => 'max-width: 150px', :alt => "default profile image", :src => image_path(UserDecorator::DEFAULT_IMAGE)}
              %p.text-info.small Note: You can upload an image on the next page.
          .col-md-8
            = render partial: 'users/edit_table', :locals => { :f => f, :user => new_instructor, :show_biography => false }
      .modal-footer
        %button.btn.btn-default{'data-dismiss' => 'modal'}
          Cancel
        %button.btn.btn-primary{:type => 'submit'}
          Create Instructor

  %div.tab-pane.active#existing-instructor
    %form{:action => team_instructors_path(team), :method => 'POST'}
      .container-fluid
        %img.center-block.img-block{:style => 'max-width: 150px', 'data-default-src' => '//storage.googleapis.com/bjjmapper/default-user-250.png', 'src' => '//storage.googleapis.com/bjjmapper/default-user-250.png'}
        %br/
        = hidden_field_tag :team_id, team.id
        = hidden_field_tag :authenticity_token, form_authenticity_token
        = text_field_tag :id, nil, class: 'instructor-name form-control input-md'
        %br/
      .modal-footer
        %button.btn.btn-default{'data-dismiss' => 'modal'}
          Cancel
        %button.btn.btn-primary{:type => 'submit'}
          Add Instructor
