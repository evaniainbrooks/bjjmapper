upstream rollfindr {
  server              unix:///tmp/rollfindr.sock;
}

server {
  listen              *:9001;
  server_name         dev.bjjmapper.com;

  listen 9002 ssl;
  ssl_certificate /etc/nginx/ssl/rollfindr.crt;
  ssl_certificate_key /etc/nginx/ssl/rollfindr.key;

  access_log          /var/log/nginx/rollfindr-access.log;

  location /favicon.ico {
    root              /var/www/rollfindr/public/assets/favicon.ico;
    gzip_static       on;
    expires           max;
    add_header        Cache-Control public;
  }

  location / {
    root              /var/www/rollfindr/public;
    try_files         $uri @app;
    gzip_static       on;
    expires           max;
    add_header        Cache-Control public;
  }

  location @app {
    proxy_pass        http://rollfindr;
    proxy_set_header  X-Real-IP  $remote_addr;
    proxy_set_header  X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header  X-Forwarded-Proto http;
    proxy_set_header  Host $http_host;
    proxy_redirect    off;
    proxy_next_upstream error timeout invalid_header http_502;
  }   
}
